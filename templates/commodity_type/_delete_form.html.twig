<form method="post" action="{{ path('app_commodity_type_delete', {'id': commodity_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commodity_type.id) }}">
    <button class="btn">Delete</button>
</form>
