<!DOCTYPE html>

<title>Commodity</title>

{% block body %}
    <h1>Commodity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commodity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ commodity.title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ commodity.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commodity_index') }}">back to list</a>

    <a href="{{ path('app_commodity_edit', {'id': commodity.id}) }}">edit</a>

    {{ include('commodity/_delete_form.html.twig') }}
{% endblock %}
